# Keep the build context small: only Go source + go.mod are needed to build.
.git
.github
*.md
docs/
data/
logs/
*.log
.env
.env.local
# Dashboard frontend deps (the prebuilt static/ is committed and embedded)
internal/dashboard/web/node_modules
# Built artifacts (the image builds its own binary)
noctra
noctra.new
noctra-pi
noctra-arm64
.DS_Store
